spring.application.name=__PROJECT_NAME__

# Populated from server/.env by scripts/mvn.mjs, with these as fallbacks.
server.port=${SERVER_PORT:3001}
app.client-origin=${CLIENT_ORIGIN:http://localhost:5173}
